{
  "src/main/hive.ts": [
    {"before":"              // Codex refuses to run hooks from a config dir without persisted\n              // \"hook trust\" (normally an interactive gate). Our hooks.json is\n              // hive-authored inside an isolated CODEX_HOME, so we bypass that gate\n              // for this automated spawn — the flag's documented use (\"automation\n              // that already vets hook sources\"). Without it the hooks silently\n              // never fire. Must precede the positional prompt.\n              preArgs.push('--dangerously-bypass-hook-trust');","after":"              // OrbiAgents leaves Codex hook trust under the operator's control.\n              // Do not claim hook telemetry is ready merely because files exist.\n              degraded = 'Codex hook trust has not been verified by OrbiAgents. Live status, cost and inbox wake may be unavailable until the operator establishes trust.';","reason":"Do not bypass Codex hook trust; explicitly report unverified hook telemetry in the spawn result."}
  ],
  "src/main/index.ts": [
    { "before": "import { app, BrowserWindow, clipboard, dialog, ipcMain, Menu, powerMonitor, powerSaveBlocker, screen, shell, Notification } from 'electron';", "after": "import './migrationBootstrap';\nimport { app, BrowserWindow, clipboard, dialog, ipcMain, Menu, powerMonitor, powerSaveBlocker, screen, shell, Notification } from 'electron';", "reason": "Configure independent migration userData and sessionData before main-process dependencies are evaluated." }
  ],
  "src/main/config.ts": [
    { "before": "  autoMode: true,", "after": "  autoMode: false,", "reason": "Require explicit opt-in to automatic provider permissions." },
    { "before": "  autoUpdate: true,", "after": "  autoUpdate: false,", "reason": "Do not automatically download upstream application updates." },
    { "before": "  telemetryEnabled: true,", "after": "  telemetryEnabled: false,", "reason": "Disable telemetry for fresh migration configuration." }
  ],
  "src/main/hiddenClaude.ts": [
    { "before": "      '--permission-mode', 'bypassPermissions',", "after": "      // OrbiAgents: retain the provider's normal permission checks.", "reason": "Hidden helper must not unconditionally bypass provider permission checks; unattended calls may now require consent or time out." }
  ]
}
